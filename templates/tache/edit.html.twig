{% extends 'base.html.twig' %}

{% block title %}Edit Tache{% endblock %}

{% block body %}
    <div class="content container-fluid"  style="margin-left: 200px;width: auto">
        <div class="page-header">
            <h3 class="page-title">Edit Tache</h3>
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-body">
                        <form method="post">
                            {{ form_widget(form) }}
                            <button type="submit" class="btn btn-primary">Update</button>
                            <a href="{{ path('app_tache_index', {'id_projet': project.getId()}) }}" class="btn btn-secondary">Back to List</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
